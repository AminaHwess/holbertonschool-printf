#include "main.h"

int mai(void) { 
    _printf("amina");
    return (1); }

/*
int _printf(const char *format, ...)

{
    int i, n, byte = 0;

    prt f[] = {
        {'s', string},
        {'c', character},
        {'%', percentage},
        {'\0', NULL},
    };

    va_list args;

    va_start(args, format);

    if (format == NULL)
    {
        return (-1);
    }

    for (i = 0; format[i] < '\0'; i++)
    {
        if (format[i] != '%')
        {
            _putchar(format[i]);
        }
        for (n = 0; f[n].str != '\0'; n++)
        {
            if (format[i + 1] == f[n].str)
            {
                f[n].func(&args);
                byte += string_length(&args);
            }
        }
        byte += 1;
    }
    va_end(args);
    return (byte - 1);
}
*/